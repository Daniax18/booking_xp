# requirements-postgres.txt
# Dépendances spécifiques pour PostgreSQL
# Voir: https://docs.sqlalchemy.org/en/20/dialects/postgresql.html

psycopg2-binary==2.9.9        # PostgreSQL adapter for Python

# Alternative (plus léger, sans binaire):
# psycopg==3.1.14             # Pure Python implementation

# Installation PostgreSQL (si vous ne l'avez pas):
# Windows: https://www.postgresql.org/download/windows/
# macOS: brew install postgresql
# Linux: apt-get install postgresql postgresql-contrib
# Docker: docker run -e POSTGRES_PASSWORD=mypassword -p 5432:5432 postgres

# Configuration dans .env:
# DATABASE_URL=postgresql://user:password@localhost:5432/inventory_db
